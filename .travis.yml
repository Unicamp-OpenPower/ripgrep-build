language: rust
os: linux-ppc64le
dist: xenial
sudo: required
rust:
  - 1.0.0

before_install:
  - sudo apt-get update

script:
- git clone https://github.com/BurntSushi/ripgrep
- cd ripgrep
- cargo build --release
- ./target/release/rg --version
